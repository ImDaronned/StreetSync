Main endpoint: 
 -Users: https://streetsync.azurewebsites.net/users
 -Events: https://streetsync.azurewebsites.net/events
 -Services: https://streetsync.azurewebsites.net/services
 -Bank: https://streetsync.azurewebsites.net/bank

Users:
 -Get Profile:
  -Link: Users/profile
  -Method: GET
  -Headers:
   -Authorization: name token(token that you got from the login)
   -Content-type: application/json
  -response: JSON format
 
 -Sign Up/Register:
  -Link: Users/signup
  -Method: POST
  -Headers:
   -Content-type: application/json
  -body:
   -password(mandatory): string with the password
   -email(mandatory): string with the email
   -firstname(mandatory): string with the first name
   -name(mandatory): string with the name
  -response: JSON format

 -Sign in/Login:
  -Link: Users/signin
  -Method: POST
  -Headers:
   -Content-type: application/json
  -body:
   -email(mandatory): string with the email
   -password(mandatory): string with the password
  -responce: JSON format

 -Update profile:
  -link: Users/profile
  -Method: PATCH
  -header:
   -Authorization: name token
   -Content-type: application/json
  -body:
   -firstname(optional): string with first name
   -name(optional): string with the name
   -email(optional): string with the email
   -password(optional): string with the password
   You need at least one argument in the body
  -responce: JSON format

 -Delete profile:
  -Link: Users/profile
  -Method: DELETE
  -header:
   -Authorization: name token
   -Content-type: application/json
  -responce: JSON format

Services:
 -Get Services:
  -link: Services
  -method: GET
  -Url: ?
   -localisation: bool to search based on the location 
   -owner: bool to search based on the owner
   -id: number to search based on the id
  -header:
   -Authorization: name token
   -Content-type: application/json
  -responce: JSON format
 
 -Post Service:
  -link: Services
  -method: POST
  -header: 
   -Authorization: name token
   -Content-type: application/json
  -body:
   -name: string with the name
   -tags: array of tags
   -price: number with the price
   -description(optional): string withe description
   -imageLink(optional): string with the imageLink
  -responce: JSON format

 -Update Service:
  -link: Services
  -Method: Patch
  -header:
   -Authorization: name token
   -Content-type: application/json
  -url: ?
   -id: to update based on the id
  -body:
   -name(optional): string with the name
   -description(optional): string with the description
   -tags(optional): array of tags
   -price(optional): number with the price
   -imageLink(optional): string with imageLink
  -responce: JSON format

 -Delete Service:
  -link: Services
  -method: DELETE
  -header: 
   -Authorization: name token
   -Content-type: application/json
  -url: ?
   -id: to delete based on the id
  -responce: JSON format

 -Post Score:
  -link: Services/score
  -method: POST
  -header:
   -Authorization: name token
   -Content-type: application/json
  -body:
   -service_id: service's id for the score
   -score: number with the score
   -description(optional): string with the description
  -responce: JSON format

 -Update Score:
  -link: Services/score
  -method: PATCH
  -header:
   -Authorization: name token
   -Content-type: application/json
  -body:
   -service_id: service's id for the score
   -score: number with the score
   -description(optional): string with the description
  -responce: JSON format

 -Get reservation:
  -link: Services/reservation
  -method: GET
  -header: 
   -Authorization: name token
  -query: ?
   -user: owner or registered
  -response: JSON format

 -Post reservation:
  -link: Services/reservation
  -method: POST
  -header:
   -Authorization: name token
   -Content-type: application/json
  -body:
   -date: string with date format(yyyy-mm-dd hh:mm)
  -query: ?
   -service_id: int
  -response: JSON format

 -Delete reservation:
  -link: Services/reservation
  -method: DELETE
  -header:
   -Authorization: name token
  -query: ?
   -service_id: int 
  -response: JSON format

 -Accept reservation:
  -link: Services/accept
  -method: POST
  -header: 
   -Authorization: name token
   -Content-type: application/json
  -body:
    -reservation_id: int
  -response: JSON format

 -Reject reservation:
  -link: Services/reject
  -method: POST
  -header:
   -Authorization: name token
   -Content-type: application/json
  -body:
    -reservation_id: int
  -response: JSON format

 -Pay reservation:
  -link: Services/pay
  -method: POST
  -header:
   -Authorization: name token 
   -Content-type: application/json
  -body:
   -reservation_id: int
  -response: JSON format

 -Get tags:
  -link: Services/tags
  -method: GET
  -response: array of string

Events:
 -Get Events:
  -link: Events
  -method: GET
  -url: ?
   -localisation: bool
   -owner: bool
   -id: number
  -header:
   -Authorization: name token
   -Content-type: application/json
  -response: JSON format

 -Post Event:
  -link: Events
  -method: POST
  -header:
   -Authorization: name token
   -Content-type: application/json
  -body:
   -name: string with the name
   -tags: array of tags
   -date: string with the date format
   -imageLink(optional): string with the image link
   -description(optional): string with the description
  -response: JSON format	
  
 -Update Event:
  -link: Events
  -method:PATCH
  -url: ?
   -id: number
  -header:
   -Authorization: name token
   -Content-type: application/json
  -body:
   -name(optional): string with the name
   -description(optional): string with the description 
   -tags(optional): array of tags
   -date(optional): string with the date format
   -imageLink(optional): string with the image link
  -response: JSON format
 
 -Delete Event:
  -link: Events
  -method: DELETE
  -url: ?
   -id: number
  -header:
   -Authorization: name token
   -Content-type: application/json
  -response: JSON format

 -Get reservation:
  -link: Events/reservations
  -method: GET
  -header: 
   -Authorization: name token
  -response: JSON format

 -Post reservation:
  -link: Events/reservation
  -method: POST
  -header:
   -Authorization: name token
   -Content-type: application/json
  -body:
   -events_id: int
  -response: JSON format

 -Get tags list:
  -link: Events/tags
  -method: GET
  -response: JSON format

-Bank:
 -Gets bank info:
  -link: Bank
  -method: GET
  -header:
   -Authorization: name token
   -Content-type: application/json
  -response: JSON format

 -Post bank info:
  -link: Bank
  -method: POST
  -header:
   -Authorization: name token
   -Content-type: application/json
  -body:
   -card_name: string with the card name
   -card_number: string with the card number 
   -card_expDate: string with the expiration date of the card
   -card_cvc: number with the card cvc
  -response: JSON format

 -Update bank info:
  -link: Bank
  -method: PATCH
  -header:
   -Authorization: name token
   -Content-type: application/json
  -body:
   -card_name: string with the card name
   -card_number: string with the card number 
   -card_expDate: string with the expiration date of the card
   -card_cvc: number with the card cvc
  -response: JSON format

 -Delete bank info:
  -link: Bank
  -method: DELETE
  -header:
   -Authorization: name token
   -Content-type: application/json
  -response JSON format

